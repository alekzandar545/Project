#include <iostream>
#include "monster.h"
#include "player.h"
int main(){
    srand(time(0));
    //std::cout << rand() % 2;
    Player* p = new Player(Player::PlayerRace::Berserk);
    Monster m(1);
    std::cout << p->hp << m.hp << '\n';
    m.Attack((Entity*)p);
    std::cout << p->hp;
}